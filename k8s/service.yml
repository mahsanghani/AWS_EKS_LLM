apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: llm-deployment
spec:
  selector:
    app: llm-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP